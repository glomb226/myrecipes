<%= render 'shared/title', title: 'Registered Chefs' %>

<div class="row">
    <div class="col-md-4 col-md-offset-4 center">
      <%= will_paginate %>
    </div>    
</div>

<% @chefs.each do |chef| %>
  <div class="row">
    <div class="col-md-4 col-md-offset-4 center">
      <section class= "chef_info center">
        <%= link_to gravatar_for(chef, size: 200), chef_path(chef) %>
        <h5>Chef Name: <%= chef.chefname %></h5>
      </section>
     </div>
  </div>
<%end%>

<div class="row">
    <div class="col-md-4 col-md-offset-4 center">
      <%= will_paginate %>
    </div>    
</div>
